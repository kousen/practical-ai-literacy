name: Build & publish slides PDF

on:
  push:
    branches: [main]
    paths:
      - 'slides.md'
      - 'slides-freshmen.md'
      - 'public/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/build-slides-pdf.yml'
  workflow_dispatch:

concurrency:
  group: slides-pdf
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Install Chromium for Playwright
        run: npx playwright install --with-deps chromium

      - name: Export slides to PDF
        run: |
          npm run export
          npm run export:freshmen

      - name: Verify PDFs were produced
        # Fail loudly: action-gh-release only warns on a missing file, so without
        # this a renamed/missing export would publish a release with no asset.
        run: |
          for f in practical-ai-literacy-slides.pdf ai-tools-for-academic-success.pdf; do
            test -s "$f" \
              || { echo "::error::$f is missing or empty — export failed or the output name changed"; exit 1; }
          done

      - name: Publish rolling "latest" release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: slides-latest
          name: Latest slides PDF
          body: |
            Auto-built at commit ${{ github.sha }}.

            - Workshop deck (`slides.md`): [`practical-ai-literacy-slides.pdf`](https://github.com/${{ github.repository }}/releases/latest/download/practical-ai-literacy-slides.pdf)
            - First-year orientation (`slides-freshmen.md`): [`ai-tools-for-academic-success.pdf`](https://github.com/${{ github.repository }}/releases/latest/download/ai-tools-for-academic-success.pdf)
          files: |
            practical-ai-literacy-slides.pdf
            ai-tools-for-academic-success.pdf
          make_latest: true
